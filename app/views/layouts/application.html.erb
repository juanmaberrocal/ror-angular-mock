<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
  <%= csrf_meta_tags %>
  <title>ROR-Angular Mock</title>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->

  <%= javascript_include_tag 'application' %>
</head>
<body ng-app="custSupportTickets">

  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" ng-href="/">RoR-Angular Mock</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-right">
          <li ng-class="{ active: currState.root == '/users' }" ng-show="currentUser.isLoggedIn && currentUser.user.admin"><a ng-href="#/users">Users</a></li>
          <li ng-class="{ active: currState.root == '/tickets' }" ng-show="currentUser.isLoggedIn"><a ng-href="#/tickets">Tickets</a></li>
          <li><a ng-show="currentUser.isLoggedIn" ng-click="logOut()">Log Out</a></li>
          <li><a ng-href="#/login" ng-show="!currentUser.isLoggedIn">Log In</a></li>
        </ul>
      </div><!--/.navbar-collapse -->
    </div>
  </nav>

  <div class="container-fluid">
    <ui-view><!-- view rendered here --></ui-view>
  </div>

  <br/>

  <footer>
  	<!-- creator info -->
  	<p class="text-center">By: Juan M Berrocal</p>
  </footer>

</body>
</html>
